<?xml version="1.0" encoding="utf-8"?>
<!--
     Шапка меню по удержанию выключателя. Две строки: сверху память, загрузка
     процессора и батарея, снизу частоты и температура. Второй строкой, а не
     в общий ряд: на экране 1280x720 шесть значений в одну строку уже не
     читаются.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="16dp"
    android:orientation="vertical">

<LinearLayout
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical">

    <!-- Memory usage text on the left -->
    <TextView
        android:id="@+id/memory_usage"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:textSize="18sp"
        android:layout_marginEnd="8dp"
        android:textColor="#FFFFFF" />

    <!-- CPU percentage text in the middle -->
    <TextView
        android:id="@+id/cpu_usage"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:textSize="18sp"
        android:textColor="#FFFFFF"
        android:gravity="center"
        android:text="CPU: 0%" />

    <!-- Battery icon -->
    <ImageView
        android:id="@+id/battery_icon"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_marginEnd="8dp"
        android:src="@drawable/ic_battery"
        android:tint="@android:color/white" />

    <!-- Battery percentage text -->
    <TextView
        android:id="@+id/battery_percentage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="18sp"
        android:layout_marginLeft="8dp"
        android:textColor="#FFFFFF" />

</LinearLayout>

    <!-- Частоты и температура. Значения числовые, переводить нечего, поэтому
         строка собирается в коде и локализации не требует. -->
    <TextView
        android:id="@+id/soc_stats"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:textSize="14sp"
        android:textColor="#B0B0B0" />

</LinearLayout>
